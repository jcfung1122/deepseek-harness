/**
 * Web power button plugin, node half.
 *
 * Deliberately empty. The power capability lives in the Host `power` gateway
 * (shutdown/restart Remote); this package only renders the browser button.
 */

/** Host plugin body — the feature lives in the browser half. */
export function apply(): void {}
