/**
 * Web color-accents plugin, node half.
 *
 * Deliberately empty. Every side effect this feature owns — the brand-blue
 * token layer and the read-only git-tag decorator — is browser-presentation
 * state that belongs to the client half; the Host has nothing to contribute.
 */

/** Host plugin body — the whole feature lives in the browser half. */
export function apply(): void {}
